# SPDX-License-Identifier: AGPL-3.0-or-later
# Test-only dependencies. Never needed on the production server.
#
#   .venv/bin/pip install -r requirements-dev.txt
-r requirements.txt

# httpx, httpcore and certifi are production dependencies now (paypal.py), so
# they are pinned in requirements.txt and come in through the -r above.
